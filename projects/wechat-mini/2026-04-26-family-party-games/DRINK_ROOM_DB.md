# 趣味抽签：云开发说明

在小程序**云开发控制台**创建下述集合、上传并部署 `drinkRoomService` 云函数。客户端通过 `drink_gameState` 的 **单文档 `.watch()`** 实时同步**同场环节态**；投票与随机响铃者在云函数中执行以保证一致性与不能重复改票。

（集合名 `drink_*` 为历史 id，界面文案已统一为「趣味抽签 / 聚会组 / 趣味小任务」。）

其余字段与权限说明以原表为准；默认组名在代码中为「聚会组」。
