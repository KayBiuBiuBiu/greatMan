# 家庭聚会助手 — 合规扫描 / 多人同步 / 云函数 / 命名债务

> 项目路径：`projects/wechat-mini/2026-04-26-family-party-games`  
> 扫描依据：仓库内实际文件（2026-05-16）。  
> `grep` 默认匹配大小写；合规关键词在 `.md` 文档中的命中一并列出，便于区分「用户可见代码」与「内部说明文档」。

---

## 一、合规风险扫描

### 1.1 关键词：`drink`、`酒`、`干杯`、`罚酒`、`喝酒`

| 文件 | 行号 | 附近内容 |
|------|------|----------|
| `豆包-家庭聚会项目说明.md` | 39 | 对照表行：甩锅酒局 / 罚酒 / 饮酒 → 趣味抽签…（**说明文档**，非小程序运行界面） |
| `豆包-家庭聚会项目说明.md` | 42 | `drinkRoomService`、`werewolf` 历史 id 说明 |
| `豆包-家庭聚会项目说明.md` | 50 | `drink-party` 路径、`drink_*` 集合名说明 |
| `DRINK_ROOM_DB.md` | 3–5 | `drink_gameState`、`drink_*` 集合、`drinkRoomService`（**运维文档**） |
| `pages/index/index.js` | 8, 11, 46, 161–163, 377, 395 | `callDrink`、`drinkParty`、跳转 `drink-party`、`drink_nick` |
| `pages/setup/setup.js` | 6, 24, 74, 126, 418–422, 434, 462, 502 | 同上 + 创建后进 `drink-party` |
| `pages/setup/setup.wxml` | 4, 15, 21, 41–42 | `drinkParty` 副文案、卡片、`wx:if` |
| `components/party-cd/party-cd.js` | 3 | 注释：`drink-party` 主逻辑 |
| `utils/drinkRoomCloud.js` | 35, 41, 63 | 错误提示、`name: 'drinkRoomService'` |
| `cloudfunctions/drinkRoomService/index.js` | 2, 9–12, 214 | 集合常量 `drink_rooms` 等、`[drinkRoomService]` 日志 |
| `cloudfunctions/drinkRoomService/package.json` | 2 | `"name": "drinkRoomService"` |
| `pages/drink-party/drink-party.js` | 全文多处 | `drink_nick`、`drink_gameState`、`watch drink` 等（见第二节附录） |
| `pages/drink-party/drink-party.wxml` | （无「酒」字） | 对外标题为「趣味抽签」，任务表述为「趣味小任务」 |
| `app.json` | 11 | 页面路径 `pages/drink-party/drink-party` |
| `data/game-data.js` | 50 | 摘要中 `drinkRoomService`、云集合说明 |

**`干杯`、`罚酒`、`喝酒`**：仅在 `豆包-家庭聚会项目说明.md` 第 39 行（对照表）出现；**业务代码与 WXML 中未发现这三词**。

**`酒`（单字）**：除上述 `豆包-家庭聚会项目说明.md` 与 `DRINK_ROOM_DB.md` / 说明性文字外，**`pages/undercover`、`pages/werewolf`、`pages/drink-party` 的 wxml/js 中无「酒」「喝」**（见第四节）。

---

### 1.2 关键词：`kill`、`杀`、`死`、`恐怖`、`血腥`

| 文件 | 行号 | 附近内容 |
|------|------|----------|
| `豆包-家庭聚会项目说明.md` | 38, 51, 59 | 产品说明：温和词、去恐怖表述、审核维度（**文档**） |
| `docs/WEREWOLF_DB.md` | 7 | 表字段说明（**文档**） |
| `cloudfunctions/werewolfService/index.js` | 298, 339–342, 369–429, 528, 558 | 变量/字段名 `wkill`、night 状态、`knife` 返回值等（**英文内部字段，非中文 UI**） |
| `pages/werewolf/werewolf.wxml` | 3 | 「无恐怖元素，家庭亲子友好…」 |
| `pages/werewolf/werewolf.wxml` | 158 | `wx:if="{{view && view.night && view.night.wkill}}"`（绑定内部字段名） |

**中文「杀」**：在 `*.js` / `*.wxml` / `*.json` 下 **未 grep 到**（不含 `.md` 时无匹配）。

**中文「死」**：同上 **未 grep 到**。

**`kill`（英文）**：仅 `werewolfService/index.js` 与 `werewolf.wxml` 中与 `wkill` 相关。

**`血腥`**：全仓库 **无匹配**。

---

### 1.3 关键词：`竞技`、`对战`、`排名`、`分数榜`

| 文件 | 行号 | 附近内容 |
|------|------|----------|
| `豆包-家庭聚会项目说明.md` | 10–11, 53 | 产品定位：无广域对战、不强调排名竞技（**文档**） |
| `README.md` | 3, 27 | 无陌生匹配、非广域排名（**文档**） |
| `pages/song-guess/song-guess.wxml` | **80** | **`<view class="h2">最终排名</view>`** — **用户可见强竞技表述，合规风险较高** |
| `pages/draw-guess/draw-guess.wxml` | 3 | 「无线上**竞技**」（用于否定线上竞技，语境为合规说明） |
| `pages/setup/setup.js` | 214, 278 | 「无线上**对战**」（说明文案） |
| `pages/setup/setup.wxml` | 4 | 「无线上**对战**」 |
| `pages/undercover/undercover.wxml` | 3 | 「无线上**竞技**」 |
| `data/game-data.js` | 134 | `'可扩展秒表、分组和**排名**'`（某条 `summary` 字段，视是否展示到 UI 需再确认） |

---

### 1.4 `pages/drink-party/drink-party.js` 是否存在与完整内容

- **存在**，路径：`pages/drink-party/drink-party.js`（未改名、未删除）。
- **完整源码见本文档末尾「附录 A」**（与仓库逐字一致，便于外发审计）。

**说明**：页面 **WXML 对外名称为「趣味抽签」**；内部仍使用 `drink_*` 集合、`drink_nick` 存储键、`sips` 等历史字段名（结果区展示为「次趣味小任务」，见 `drink-party.wxml` 约 157–159 行）。

---

## 二、多人同步核心逻辑

### 2.1 说明：`undercover` 页投票函数名

- 仓库中 **没有名为 `submitVote` 的前端方法**。
- 投票提交对应方法为 **`submitMyVote`**，内部 `wx.cloud.callFunction` 的 `action` 为 **`submitVote`**（与云函数一致）。

### 2.2 `pages/undercover/undercover.js` — `submitMyVote`（投票提交）

```254:269:pages/undercover/undercover.js
  submitMyVote() {
    const t = this._voteOid
    if (!t) {
      wx.showToast({ title: '先点选一人', icon: 'none' })
      return
    }
    callUndercoverService(
      { action: 'submitVote', roomId: this.data.roomId, targetOpenId: t },
      {
        onOk: () => {
          this._voteOid = ''
          this.loadView()
        },
        onError: () => {}
      }
    )
  },
```

### 2.3 `onSnapshot` / `watch` 监听

- **`onSnapshot`**：全项目 **无引用**（已 `grep`）。
- **`watch`**：`uc_state` 单文档监听，在 **`startWatch`** 中：

```320:364:pages/undercover/undercover.js
  unwatch() {
    if (this._w) {
      this._w.close()
      this._w = null
    }
  },
  startWatch() {
    this.unwatch()
    if (!this.data.roomId) {
      return
    }
    if (!wx.cloud) {
      return
    }
    if (!ensureUndercoverCloud()) {
      return
    }
    const db = wx.cloud.database()
    this._w = db
      .collection('uc_state')
      .doc(String(this.data.roomId))
      .watch({
        onChange: (s) => {
          const d = s && (s.data != null ? s.data : s.doc)
          if (d) {
            const mp = d.maxPlayers | 0
            const si = mp > 0 ? SIZ.indexOf(mp) : -1
            const cph = d.currentPhase || ''
            this.setData({
              state: d,
              logText: (d.publicLog || []).join('\n'),
              sizeIndex: si >= 0 ? si : 2,
              inVote: cph === 'vote' || cph === 'vote_tie',
              inVoteTie: cph === 'vote_tie',
              inDiscuss: cph === 'discuss',
              inWord: cph === 'word',
              inWaiting: cph === 'waiting' || cph === 'lobby' || !cph
            })
            this.loadView()
          }
        },
        onError: (err) => {
          console.error('[uc_state watch]', err)
        }
      })
  },
```

### 2.4 `onUnload`（清理监听）

```53:55:pages/undercover/undercover.js
  onUnload() {
    this.unwatch()
  },
```

---

### 2.5 `cloudfunctions/undercoverRoomService/index.js`

#### 主持权限校验：`assertHost`

```192:198:cloudfunctions/undercoverRoomService/index.js
async function assertHost(rid) {
  const o = await oid()
  const room = await gRoom(rid)
  if (!room || room.hostOpenId !== o) {
    throw new Error('仅房主可执行')
  }
  return o
}
```

#### 「回合 / 阶段」切换（与题目中的 `nextRound` / `startRound` 对应关系）

本云函数 **没有** 名为 `nextRound` / `startRound` 的 action。最接近的语义为：

- **开局 / 进入发词阶段**：`startGame`（`currentPhase: 'word'`、`currentRound: 1`）。
- **讨论 / 发言推进**：`hostToDiscuss`、`hostNextSpeak`。
- **进入投票**：`startVote`；平票再投：`startTieVote`。
- **投票结束与计票、放逐、进入下一轮讨论或终局**：**`endVote`**（内含票数聚合与分支逻辑，相当于「统计 + 状态机推进」）。

**`startGame` 片段：**

```332:383:cloudfunctions/undercoverRoomService/index.js
  if (a === 'startGame') {
    const rid = e.roomId
    await assertHost(rid)
    const room = await gRoom(rid)
    if (room.status !== 'waiting') {
      throw new Error('已开局')
    }
    const pl = await gPlayers(rid)
    if (pl.length < 3) {
      throw new Error('至少3人')
    }
    if (pl.length !== (room.maxPlayers | 0) && (room.maxPlayers | 0) > 0) {
      throw new Error('人未满' + (room.maxPlayers | 0) + '，暂不可开')
    }
    const p0 = PAIRS[((Math.random() * PAIRS.length) | 0) % PAIRS.length]
    // ... 分配 role/word ...
    const rMerge = {
      status: 'playing',
      currentRound: 1,
      currentPhase: 'word',
      pair: p0,
      speakIndex: 0,
      speakOrder: shuf(
        pSh
          .filter((x) => x.isAlive)
          .map((x) => x.openId)
      ),
      tieBreakOids: [],
      publicLog: ['发词完成，大家查看词语。']
    }
    await saveR(Object.assign({}, await gRoom(rid), rMerge))
    return { ok: 1, pair: p0 }
  }
```

**投票统计与放逐（`endVote` 核心：计票 → 平票 / 放逐 / 下一讨论轮 / 终局）** — 篇幅较长，以下为入口与计票起点：

```527:565:cloudfunctions/undercoverRoomService/index.js
  if (a === 'endVote') {
    const rid = e.roomId
    await assertHost(rid)
    const room = await gRoom(rid)
    const pls = (await gPlayers(rid)).slice()
    const ph = room.currentPhase
    if (ph !== 'vote' && ph !== 'vote_tie') {
      throw new Error('非投票中')
    }
    const alive = pls.filter((p) => p.isAlive)
    for (const v of alive) {
      if (!v.currentVote) {
        throw new Error('未全员投票')
      }
    }
    const c = {}
    if (ph === 'vote_tie') {
      const tb = room.tieBreakOids || []
      for (const v of alive) {
        const t1 = v.currentVote
        if (tb.length && tb.indexOf(t1) < 0) {
          throw new Error('有非法票')
        }
        c[t1] = (c[t1] | 0) + 1
      }
    } else {
      for (const v of alive) {
        const t1 = v.currentVote
        c[t1] = (c[t1] | 0) + 1
      }
    }
    const sorted = Object.keys(c).map((k) => ({ o: k, n: c[k] }))
    sorted.sort((a, b) => b.n - a.n)
    // ... 后续：平票分支、放逐、currentRound+1、ended 等 ...
```

**参与者提交票（云侧 `submitVote` action）：**

```486:525:cloudfunctions/undercoverRoomService/index.js
  if (a === 'submitVote') {
    const rid = e.roomId
    const t0 = String(e.targetOpenId || '')
    if (!t0) {
      throw new Error('请选择')
    }
    const pls = await gPlayers(rid)
    const me = pls.find((p) => p.openId === o)
    const room = await gRoom(rid)
    const ph = room.currentPhase
    if (!me || me.isAlive === false) {
      throw new Error('无投票权')
    }
    if (ph !== 'vote' && ph !== 'vote_tie') {
      throw new Error('非投票阶段')
    }
    if (ph === 'vote' && t0 === o) {
      throw new Error('勿投自己')
    }
    if (ph === 'vote_tie') {
      const tb = room.tieBreakOids || []
      if (tb.length && tb.indexOf(t0) < 0) {
        throw new Error('只许投平票者')
      }
    }
    const tPl = pls.find((p) => p.openId === t0)
    if (!tPl || tPl.isAlive === false) {
      throw new Error('目标已出局')
    }
    if (me.currentVote) {
      throw new Error('已投票')
    }
    await db
      .collection(UC_P)
      .doc(me._id)
      .update({ data: { currentVote: t0, updatedAt: t() } })
    const r = await gRoom(rid)
    const pl2 = await gPlayers(rid)
    await setState(r, pl2)
    return { ok: 1 }
  }
```

---

### 2.6 `cloudfunctions/roomService/index.js` — 创建 / 加入 /「转移主持权」

#### 创建「聚会组」（4 位口令，`rooms` 集合）

```62:97:cloudfunctions/roomService/index.js
async function createRoom(event) {
  const openId = await getOpenId()
  const now = Date.now()
  const nickName = normalizeNickName(event.nickName)
  const avatarUrl = normalizeAvatarUrl(event.avatarUrl)
  const selectedGame = event.selectedGame || null
  const status = event.status || 'waiting'
  let roomCode = createRoomCode()

  for (let i = 0; i < 8; i += 1) {
    const existing = await findActiveRoom(roomCode)
    if (!existing) break
    roomCode = createRoomCode()
  }

  const room = {
    roomCode,
    hostOpenId: openId,
    players: [
      {
        openId,
        nickName,
        avatarUrl,
        isHost: true,
        joinedAt: now
      }
    ],
    selectedGame,
    status,
    createdAt: now,
    updatedAt: now
  }

  const result = await db.collection(ROOMS).add({ data: room })
  return publicRoom(Object.assign({}, room, { _id: result._id }), openId)
}
```

#### 加入聚会组

```99:148:cloudfunctions/roomService/index.js
async function joinRoom(event) {
  const openId = await getOpenId()
  const now = Date.now()
  const roomCode = normalizeRoomCode(event.roomCode)
  const nickName = normalizeNickName(event.nickName)
  const avatarUrl = normalizeAvatarUrl(event.avatarUrl)

  if (roomCode.length !== 4) {
    throw new Error('请输入4位口令')
  }

  const room = await findActiveRoom(roomCode)
  if (!room) {
    throw new Error('聚会组不存在或已过期')
  }

  const players = room.players || []
  const existed = players.some(function (player) {
    return player.openId === openId
  })

  const nextPlayers = existed
    ? players.map(function (player) {
      if (player.openId === openId) {
        return Object.assign({}, player, { nickName, avatarUrl })
      }
      return player
    })
    : players.concat([
      {
        openId,
        nickName,
        avatarUrl,
        isHost: openId === room.hostOpenId,
        joinedAt: now
      }
    ])

  await db.collection(ROOMS).doc(room._id).update({
    data: {
      players: nextPlayers,
      updatedAt: now
    }
  })

  return publicRoom(Object.assign({}, room, {
    players: nextPlayers,
    updatedAt: now
  }), openId)
}
```

#### 转移主持权

**结论：`roomService/index.js` 中不存在「转移主持权 / 更换 hostOpenId」的独立 action 或函数。**  
`hostOpenId` 仅在 **`createRoom`** 时设为创建者；`joinRoom` 只更新 `players` 列表，**不会**把 `room.hostOpenId` 改给后加入者。其他操作（如 `startRoom`、`undercoverAssign`）仅校验 `room.hostOpenId === openId`。

---

## 三、云函数健康度

### 3.1 `app.js` — 云开发初始化

**当前 `app.js` 内没有 `wx.cloud.init` / `cloud.init` 调用**，仅注释说明由 `utils/roomCloud` 等在首次需要时再初始化：

```1:11:app.js
App({
  onLaunch() {
    // 云能力见 utils/roomCloud：首次点「输入口令/生成口令」再 wx.cloud.init，减少冷启动时误报
  },

  globalData: {
    appName: '家庭聚会助手',
    // 由 utils/roomCloud 在首次需要「口令」云能力时置 true（避免未开通云时启动就 init 打日志 / timeout）
    cloudInited: false
  }
})
```

**实际小程序端初始化**在 `utils/roomCloud.js` 的 `ensureCloudInit`、以及 `utils/undercoverRoomCloud.js` 的 `ensureCloud()`（二者逻辑等价：按需 `wx.cloud.init`）。

```31:57:utils/roomCloud.js
function ensureCloudInit() {
  if (!wx.cloud) {
    return false
  }
  const app = getApp && getApp()
  if (app && app.globalData && app.globalData.cloudInited) {
    return true
  }
  const opts = { traceUser: true }
  if (configEnv) {
    opts.env = configEnv
  } else if (wx.cloud.DYNAMIC_CURRENT_ENV != null) {
    opts.env = wx.cloud.DYNAMIC_CURRENT_ENV
  }
  if (debugCloudLog) {
    /* eslint-disable no-console */
    console.log('[roomService] wx.cloud.init 首次调用', {
      hasConfigEnv: !!configEnv,
      hasDynamic: wx.cloud.DYNAMIC_CURRENT_ENV != null
    })
    /* eslint-enable no-console */
  }
  wx.cloud.init(opts)
  if (app && app.globalData) {
    app.globalData.cloudInited = true
  }
  return true
}
```

### 3.2 `utils/undercoverRoomCloud.js` — 调用云函数封装

```15:112:utils/undercoverRoomCloud.js
function ensureCloud() {
  if (!wx.cloud) {
    return false
  }
  const app = getApp && getApp()
  if (app && app.globalData && app.globalData.cloudInited) {
    return true
  }
  const o = { traceUser: true }
  if (configEnv) {
    o.env = configEnv
  } else if (wx.cloud.DYNAMIC_CURRENT_ENV != null) {
    o.env = wx.cloud.DYNAMIC_CURRENT_ENV
  }
  wx.cloud.init(o)
  if (app && app.globalData) {
    app.globalData.cloudInited = true
  }
  return true
}

// ... formatCallFunctionFail ...

function callUndercoverService(data, opts) {
  const { onOk, onError, silent } = opts || {}
  if (!wx.cloud) {
    wx.showToast({ title: '无云能力', icon: 'none' })
    onError && onError()
    return
  }
  if (!ensureCloud()) {
    onError && onError()
    return
  }
  const id = ++seq
  wx.cloud.callFunction({
    name: 'undercoverRoomService',
    data,
    success: (res) => {
      const r = (res && res.result) || {}
      if (r && r.errMsg) {
        if (!silent) {
          wx.showToast({ title: String(r.errMsg), icon: 'none', duration: 3500 })
        }
        onError && onError(new Error(String(r.errMsg)), { result: r })
        return
      }
      onOk && onOk(res)
    },
    fail: (err) => {
      if (!silent) {
        const title = formatCallFunctionFail(err)
        console.error('[undercoverRoomService callFunction fail]', err)
        wx.showToast({ title, icon: 'none', duration: 4500 })
      }
      onError && onError(err)
    }
  })
}

module.exports = { callUndercoverService, ensureUndercoverCloud: ensureCloud }
```

### 3.3 `cloudfunctions/` 目录与 `package.json` 依赖

| 云函数目录 | `dependencies` 是否为空 | 是否包含 `wx-server-sdk` |
|------------|-------------------------|---------------------------|
| `roomService` | 否 | 是（`"wx-server-sdk": "latest"`） |
| `drinkRoomService` | 否 | 是 |
| `drawRoomService` | 否 | 是 |
| `musicRoomService` | 否 | 是 |
| `gameStatsService` | 否 | 是 |
| `undercoverRoomService` | 否 | 是 |
| `werewolfService` | 否 | 是 |

**结论**：所列 7 个云函数的 `package.json` **均非空**且 **均声明 `wx-server-sdk`**。

**云函数内初始化示例**（`undercoverRoomService`）：

```5:7:cloudfunctions/undercoverRoomService/index.js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()
```

---

## 四、命名与历史债务

### 4.1 `drink_` 前缀（集合、变量、路径）

已命中位置汇总（与 1.1 重叠部分不重复展开）：

- **集合名**：`drink_rooms`、`drink_players`、`drink_gameState`、`drink_votes` — `cloudfunctions/drinkRoomService/index.js` 第 9–12 行。
- **前端直连集合**：`pages/drink-party/drink-party.js` 中 `collection('drink_gameState')`。
- **存储键**：`drink_nick` — `pages/index/index.js`、`pages/setup/setup.js`、`pages/drink-party/drink-party.js`。
- **页面路径**：`pages/drink-party/drink-party`（`app.json`）。
- **云函数名**：`drinkRoomService`（目录名、`package.json`、`utils/drinkRoomCloud.js`）。

### 4.2 `undercover` / `werewolf` / `draw-guess` 中「酒」「喝」

对 `pages/undercover/**`、`pages/werewolf/**`、`pages/draw-guess/**` 执行 **`酒`、`喝`** 搜索：**无匹配**。

---

## 五、审计摘要（给产品 / 审核的短结论）

1. **饮酒相关中文**：运行界面 WXML/主要 JS **未见「酒」「喝」「罚酒」「干杯」**；历史命名仍为 `drink-*` / `sips` 等，与对外「趣味抽签」并存，建议在后续迭代中逐步统一字段名（工程成本见 `豆包-家庭聚会项目说明.md`）。
2. **暴力恐怖向中文**：业务代码中 **未见「杀」「死」「血腥」**；身份推理云函数仍使用 **`wkill` 等英文内部字段**，`werewolf.wxml` 有「无恐怖元素」声明，但绑定仍暴露 `wkill` 字段名（若审核关注「代码层命名」可单列）。
3. **竞技 / 排名**：`song-guess.wxml` 存在 **「最终排名」** 标题，与全局「弱化排名」口径不一致，**建议优先改掉或改为「本环节参考」类表述**。
4. **多人同步**：`undercover` 使用 **`uc_state` + `.watch()`**；`onUnload` 调用 `unwatch()`；无 `onSnapshot`。注意 **`watch` 与 `loadView` 轮询并用**，需关注频繁 `getView` 带来的费用与竞态（属架构级注意点，非本次语法错误）。
5. **`roomService`**：**无主持权转移**；4 位口令房与 6 位 `undercover` / `drink` 等云体系并存，文档需区分。

---

## 附录 A — `pages/drink-party/drink-party.js` 全文

```javascript
const { callDrink, ensure } = require('../../utils/drinkRoomCloud')
function defNick() {
  return (wx.getStorageSync('drink_nick') || '参与者').toString()
}
function fromWatch(s) {
  return s && (s.data != null ? s.data : s.doc)
}
Page({
  data: {
    opBusy: false,
    roundDisp: 0,
    roomId: '',
    roomCode: '',
    joinCode: '',
    nick: defNick(),
    state: null,
    isHost: false,
    iAmRinger: false,
    voteDone: false,
    pick: '',
    cdLabel: '3',
    deadLeftS: 0,
    wrongNames: '',
    roomNameEdit: '',
    myOpenId: ''
  },
  _w: null,
  _tcd: null,
  _tdead: null,
  _tfin: null,
  _my: '',
  _vibR: 0,
  _revR: 0,
  _finR: 0,
  _revealBusy: false,
  onLoad(q) {
    const id = (q && q.roomId) ? String(q.roomId) : ''
    const code = (q && q.roomCode) ? String(q.roomCode) : ''
    this.setData({
      joinCode: code
        .replace(/\D/g, '')
        .slice(0, 6),
      roomId: id,
      roomCode: code
    })
    if (id) {
      this._bootInRoom()
    } else {
      this.fetchMyOpenId()
    }
  },
  onUnload() {
    this.clearT()
    this.unwatch()
  },
  onHide() {
    this.clearT()
  },
  unwatch() {
    if (this._w) {
      try {
        this._w.close()
      } catch (e) {}
      this._w = null
    }
  },
  clearT() {
    if (this._tcd) {
      clearInterval(this._tcd)
      this._tcd = null
    }
    if (this._tdead) {
      clearInterval(this._tdead)
      this._tdead = null
    }
    if (this._tfin) {
      clearTimeout(this._tfin)
      this._tfin = null
    }
  },
  fetchMyOpenId() {
    if (!wx.cloud || !ensure()) {
      return
    }
    callDrink(
      { action: 'getOpenId' },
      { silent: true, onOk: (res) => {
        const o = (res && res.result && res.result.openId) || ''
        this._my = o
        this.setData({ myOpenId: o || '' })
        if (this.data.state) {
          this._applyS(this.data.state)
        }
      } }
    )
  },
  _bootInRoom() {
    this.fetchMyOpenId()
    setTimeout(() => this.fetchMyOpenId(), 200)
    this._startW()
    if (wx.cloud && ensure()) {
      const db = wx.cloud.database()
      db
        .collection('drink_gameState')
        .doc(String(this.data.roomId))
        .get()
        .then((d) => {
          if (d && d.data) {
            this._applyS(d.data)
          }
        })
    }
  },
  _startW() {
    this.unwatch()
    if (!this.data.roomId || !wx.cloud || !ensure()) {
      return
    }
    const id = String(this.data.roomId)
    const db = wx.cloud.database()
    this._w = db
      .collection('drink_gameState')
      .doc(id)
      .watch({
        onChange: (s) => {
          const d = fromWatch(s)
          if (d) {
            this._applyS(d)
          }
        },
        onError: (e) => {
          console.error('watch drink', e)
        }
      })
  },
  _applyS(d) {
    const rDisp = (d && d.currentRound) | 0
    this.setData(
      { state: d, roomNameEdit: (d && d.roomName) || '聚会组', roundDisp: rDisp },
      () => {
      const my = this._my || (this.data.myOpenId || '')
      this._my = my
      const isHost = d && d.hostOpenId && my && d.hostOpenId === my
      const rPh = d && d.phase
      const iAmRinger =
        !!(my && d && d.targetOpenId && d.targetOpenId === my && rPh === 'voting')
      const m = (d && d.votesByVoter) || {}
      const myC = m[my]
      const voteDone = !!(my && myC != null && myC !== '')
      let wrongNames = ''
      if (d && d.result && d.result.wrongVoters && d.result.wrongVoters.length) {
        wrongNames = d.result.wrongVoters
          .map((w) => `${w.nickName}（${w.sips} 次）`)
          .join('、')
      }
      if (rPh === 'voting' && d && d.targetOpenId) {
        this._revR = d.currentRound | 0
      }
      this.setData({ isHost, iAmRinger, voteDone, wrongNames })
      this._maybeVibe(d)
      this._onPhaseTick(d, rPh)
    })
  },
  _maybeVibe(d) {
    const r = d && d.currentRound | 0
    if (!d || d.phase !== 'voting' || !d.targetOpenId) {
      return
    }
    if (r === this._vibR) {
      return
    }
    this._vibR = r
    const my = this._my
    if (my && d.targetOpenId === my) {
      this._ringerVibrate()
    }
  },
  /**
   * 仅短震、不播放任何音频
   */
  _ringerVibrate() {
    for (let k = 0; k < 3; k += 1) {
      setTimeout(() => {
        try {
          wx.vibrateShort()
        } catch (e) {}
      }, k * 150)
    }
  },
  _onPhaseTick(d, ph) {
    this.clearT()
    if (!d) {
      return
    }
    if (ph === 'countdown' && d.countdownEndsAt) {
      const endT = d.countdownEndsAt
      this._tcd = setInterval(() => {
        const st2 = this.data.state
        if (!st2 || st2.phase !== 'countdown') {
          return
        }
        const remS = (endT - Date.now()) / 1000
        if (remS > 0) {
          const s = Math.min(3, Math.max(1, Math.ceil(remS)))
          const m = { 1: '1', 2: '2', 3: '3' }
          this.setData({ cdLabel: m[s] || String(s) })
        } else {
          this.setData({ cdLabel: 'Go！' })
        }
        if (Date.now() + 20 >= endT) {
          this._tryReveal()
        }
      }, 90)
    } else if (ph === 'voting' && d.votingDeadline) {
      this._tdead = setInterval(() => {
        const st2 = this.data.state
        if (!st2 || st2.phase !== 'voting') {
          return
        }
        const n = st2.votingDeadline
        const r = st2.currentRound | 0
        const left = Math.max(0, ((n - Date.now()) / 1000) | 0)
        this.setData({ deadLeftS: left })
        if (st2.voteProgress && st2.voteProgress.cast >= st2.voteProgress.need) {
          this._tryFin(r, true)
        } else if (Date.now() + 300 >= n) {
          this._tryFin(r, true)
        }
      }, 350)
      const n0 = d.votingDeadline
      const left0 = Math.max(0, ((n0 - Date.now()) / 1000) | 0)
      this.setData({ deadLeftS: left0 })
    } else {
      this.setData({ deadLeftS: 0, cdLabel: '3' })
    }
  },
  _tryReveal() {
    const d = this.data.state
    if (!d || d.phase !== 'countdown' || this._revealBusy) {
      return
    }
    if (Date.now() < (d.countdownEndsAt | 0) - 100) {
      return
    }
    this._revealBusy = true
    callDrink(
      { action: 'revealRinger', roomId: this.data.roomId },
      {
        silent: true,
        onOk: () => { this._revealBusy = false },
        onError: () => { this._revealBusy = false }
      }
    )
  },
  _tryFin(round, once) {
    if (this._finR === round) {
      return
    }
    this._finR = round
    callDrink(
      { action: 'finalizeVoting', roomId: this.data.roomId, force: false },
      { silent: true, onError: () => { if (once) { this._finR = 0 } } }
    )
  },
  onNick(e) {
    this.setData({ nick: String((e && e.detail && e.detail.value) || '').slice(0, 12) })
  },
  onCode(e) {
    this.setData({ joinCode: (e && e.detail && e.detail.value || '')
      .toString()
      .replace(/\D/g, '')
      .slice(0, 6) })
  },
  onRoomName(e) {
    this.setData({ roomNameEdit: String((e && e.detail && e.detail.value) || '')
      .slice(0, 20) })
  },
  doCreate() {
    if (!wx.cloud) {
      wx.showToast({ title: '需开通云', icon: 'none' })
      return
    }
    if (!ensure()) {
      return
    }
    const n = (this.data.nick || '参与者').trim().slice(0, 12) || '参与者'
    wx.setStorageSync('drink_nick', n)
    this.setData({ opBusy: true })
    callDrink(
      { action: 'create', nickName: n },
      {
        onOk: (res) => {
          this.setData({ opBusy: false })
          const r = (res && res.result) || {}
          if (r.errMsg) {
            return
          }
          this.setData({
            roomId: String(r.roomId),
            roomCode: r.roomCode,
            roomNameEdit: '聚会组'
          })
          this._bootInRoom()
        },
        onError: () => {
          this.setData({ opBusy: false })
        }
      }
    )
  },
  doJoin() {
    if (!wx.cloud || !ensure()) {
      return
    }
    const c = (this.data.joinCode || '')
      .replace(/\D/g, '')
      .slice(0, 6)
    if (c.length !== 6) {
      wx.showToast({ title: '6 位', icon: 'none' })
      return
    }
    const n = (this.data.nick || '参与者').trim().slice(0, 12) || '参与者'
    wx.setStorageSync('drink_nick', n)
    this.setData({ opBusy: true })
    callDrink(
      { action: 'join', roomCode: c, nickName: n },
      {
        onOk: (res) => {
          this.setData({ opBusy: false })
          const r = (res && res.result) || {}
          if (r.errMsg) {
            return
          }
          this.setData({ roomId: String(r.roomId), roomCode: c, joinCode: c })
          this._bootInRoom()
        },
        onError: () => {
          this.setData({ opBusy: false })
        }
      }
    )
  },
  onCopy() {
    const c = (this.data.state && this.data.state.roomCode) || this.data.roomCode
    if (!c) {
      return
    }
    wx.setClipboardData({ data: String(c) })
  },
  onSaveRoomName() {
    if (!this.data.isHost) {
      return
    }
    callDrink(
      { action: 'setRoomName', roomId: this.data.roomId, roomName: this.data.roomNameEdit },
      { onOk: () => wx.showToast({ title: '已保存', icon: 'none' }) }
    )
  },
  onStart() {
    this.setData({ opBusy: true })
    callDrink(
      { action: 'startRound', roomId: this.data.roomId },
      {
        onOk: () => {
          this.setData({ opBusy: false })
          this._finR = 0
        },
        onError: () => { this.setData({ opBusy: false }) }
      }
    )
  },
  onForceEnd() {
    this.setData({ opBusy: true })
    callDrink(
      { action: 'finalizeVoting', roomId: this.data.roomId, force: true },
      { onOk: () => { this.setData({ opBusy: false }) },
        onError: () => { this.setData({ opBusy: false }) } }
    )
  },
  onNextRound() {
    this.setData({ opBusy: true })
    callDrink(
      { action: 'nextRound', roomId: this.data.roomId },
      {
        onOk: () => {
          this.setData({ opBusy: false })
          this._finR = 0
          this._vibR = 0
          this._revR = 0
        },
        onError: () => { this.setData({ opBusy: false }) }
      }
    )
  },
  onCompPick(e) {
    if (this.data.voteDone) {
      return
    }
    const oid = (e && e.detail && e.detail.toOpenId) || ''
    if (oid) {
      this.setData({ pick: oid })
    }
  },
  onSubmitVote() {
    if (!this.data.pick || this.data.voteDone) {
      return
    }
    callDrink(
      { action: 'submitVote', roomId: this.data.roomId, toOpenId: this.data.pick }
    )
  },
  onAbstain() {
    if (this.data.voteDone) {
      return
    }
    callDrink({ action: 'submitAbstain', roomId: this.data.roomId })
  }
})
```

---

*文档结束。*
