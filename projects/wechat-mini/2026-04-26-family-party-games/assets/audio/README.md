# 趣味抽签响铃提示音

把你的 `ring.mp3`（1–2 秒）**复制到本目录**：

```
assets/audio/ring.mp3
```

无需上传云存储、也无需配置网址。重新编译小程序后，响铃者进入投票阶段会自动播放。

若文件缺失，仍会震动 + 红屏闪动，只是没有声音。

可选：若必须用网络地址，可在 `cloud-env.js` 设置 `drinkRingAudioUrl` 覆盖默认路径。
