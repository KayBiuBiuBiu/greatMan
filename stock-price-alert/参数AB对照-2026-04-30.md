# 参数 A/B 对照（自动执行版）

## 已自动落地（本轮）

- `trend_slippage_alert.min_volume_ratio`: `0 -> 0.8`
- `trend_slippage_alert.min_float_mv_yi`: `0 -> 5`
- `performance.process_watch_max_workers`: `1 -> 2`
- 清理 `performance` 中已无效的 Hub 预热参数（`realtime_hub.enabled=false` 时不生效）

目标：在不改核心交易逻辑前提下，先降噪并保持轮询性能。

## 已验证

- 配置合并校验通过（`merge_full_config`）。
- 实测 `python run_alert.py --once --no-notify --poll-when-closed`：`[轮询完成] 耗时 1.13s`。

## 下一轮（建议 3-5 个交易日后自动推进）

### 观察指标

- `backtest_alerts.py` 的 `trend_slip` / `drawdown` `hit_rate`
- 每日主观噪音：误报条数、是否打扰
- 漏报样本：明显走弱但未触发

### 升级条件（触发才改）

- 若噪音仍高：`min_volume_ratio` 从 `0.8 -> 1.0`
- 若小盘扰动仍多：`min_float_mv_yi` 从 `5 -> 10`
- 若误报仍明显且可接受漏报：`min_pillars_weak` 从 `2 -> 3`

### 保守回滚条件

- 若漏报明显增多：先回滚 `min_volume_ratio` 到 `0.6` 或 `0.5`
- 若优质池过度收缩：先回滚 `min_float_mv_yi` 到 `3` 或 `0`

## 快速命令

```bash
cd /Users/haha/greatMan/stock-price-alert
python run_alert.py --once --no-notify --poll-when-closed
python backtest_alerts.py -c config.json --since 2026-04-30 --json-out first_report.json
```

