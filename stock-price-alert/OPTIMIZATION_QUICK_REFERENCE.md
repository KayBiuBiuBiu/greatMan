# 优化方案快速参考

## 一句话总结每个方案

| # | 方案 | 问题 | 目标 | 难度 | ROI |
|---|------|------|------|------|-----|
| 1 | 配置合并重构 | 25 处 dict.update 重复 | 递归 merge 工具库 | 2/5 | ★★★★★ |
| 2 | 限流器优化 | 3 个独立限流器，忙轮询 | token bucket 统一限流 | 3/5 | ★★★★★ |
| 3 | 并发选股改进 | 全市场 3000+ 股票全并行，争抢限流 | 批处理（10-20 批） | 3/5 | ★★★★★ |
| 4 | 大函数拆分 | main 1363 行、process_watch 1010 行 | 各函数 <300 行 | 3/5 | ★★★★☆ |
| 5 | 重复代码消除 | 3 个 emit 函数、验证逻辑重复 | 统一到公共库 | 2/5 | ★★★★☆ |
| 6 | API 缓存重用 | stock_basic 每次启动重新解析 | mtime + LRU 缓存 | 2/5 | ★★★☆☆ |
| 7 | 异常处理标准化 | 3 种指数退避实现 | 统一异常体系 + 装饰器 | 3/5 | ★★★☆☆ |
| 8 | 测试覆盖提升 | 关键函数无单元测试 | 覆盖 40% → 65%+ | 3/5 | ★★★☆☆ |

---

## 实施优先级矩阵

```
收益 ↑
     │  ★ 配置合并     ★ 限流器      ★ 并发选股
     │  ★ 重复代码     ★ 大函数
     │           ★ 测试
     │  ★ 缓存      ★ 异常处理
     └─────────────────────────→ 难度
```

**立即做（Purple）**: 配置合并、重复代码
**快速做（Red）**: 限流器、并发选股、大函数
**后续做（Blue）**: 缓存、异常、测试

---

## 核心指标对标

### 启动性能
- 配置合并: -5%
- 缓存重用: -5%
- **小计: -10%**

### 选股性能
- 限流器: -15% 等待
- 并发批处理: -20%
- **小计: -30%+**

### 代码质量
- 大函数拆分: +25% 覆盖率
- 异常标准化: 一致性 ↑
- **小计: 大幅改善**

---

## 最小可行优化集（MVP）

若时间有限，优先做这三个：
1. **配置合并重构** (6-8h) → 启动 -5-10%
2. **Tushare 限流器** (8-10h) → 选股 -15% 等待
3. **重复代码消除** (4-6h) → 代码质量 ↑

**总投入**: 18-24 小时 = 2-3 天  
**预期收益**: 启动 -5%, 选股等待 -15%, 代码清晰

---

## 关键决策点

| 决策 | 建议 |
|------|------|
| 分阶段还是一次性? | 分阶段 (Phase 1-3) |
| 需要新依赖吗? | 无，仅使用标库 |
| 有风险吗? | 限流器需压力测试，其他低风险 |
| 需要改 config schema 吗? | 不需要，保持兼容 |
| 何时上线? | Phase 1 立即，Phase 2-3 灰度 |

---

## 检查清单

启动优化前：
- [ ] 当前基准性能已记录（启动、选股、API 调用数）
- [ ] 压力测试环境就绪（模拟 3000+ 股票并发）
- [ ] Code Review 团队已确认
- [ ] 回滚方案已准备

优化完成后：
- [ ] 新性能指标已对标
- [ ] 测试覆盖率已验证
- [ ] 文档已更新
- [ ] 团队培训已完成

---

## 文件位置参考

```
stock-price-alert/
  ├── run_alert.py (9850 行)
  │   ├── merge_full_config() [L1786-2103] ← 配置合并
  │   ├── main() [L8484+] ← 大函数拆分目标
  │   ├── process_watch_pack() [L7471+] ← 拆分目标
  │   └── _handle_runtime_command() [L4634+] ← 拆分目标
  │
  ├── quote_tushare.py (3557 行)
  │   ├── _acquire_tushare_daily_slot() [L147+] ← 限流器
  │   ├── _acquire_tushare_adj_factor_slot() [L117+] ← 限流器
  │   └── _acquire_sw_daily_slot() [L93+] ← 限流器
  │
  ├── quant_core/selector.py (2116 行)
  │   └── run_daily_selector() [L1686+, 并发 L1942] ← 并发选股
  │
  └── OPTIMIZATION_PLAN.md [NEW] ← 详细方案
```

---

## 相关命令

```bash
# 性能基准测试
time python run_alert.py --verify-config

# 选股性能测试
time python quant_cli.py daily-select --limit 100

# 并发压力测试
python -c "from quant_core.selector import run_daily_selector; ..."

# 代码统计
find . -name "*.py" | xargs wc -l | sort -n

# 覆盖率报告
pytest --cov=. tests/ --cov-report=html
```

---

文档: `/Users/haha/greatMan/stock-price-alert/OPTIMIZATION_PLAN.md`
生成时间: 2026-06-03
版本: 1.0
